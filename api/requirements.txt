# Phase 1 API Dependencies — ChemEng Thermodynamic API
# All pinned to stable versions tested with CoolProp 6.4+

# ---- Core calculation engine ----
CoolProp>=6.4.1,<7.0

# ---- Web framework ----
fastapi>=0.111.0,<1.0
uvicorn[standard]>=0.29.0,<1.0  # ASGI server with WebSocket support
python-multipart>=0.0.9          # For form data

# ---- Data validation ----
pydantic>=2.7.0,<3.0

# ---- Numerics ----
numpy>=1.24.0,<3.0

# ---- Testing ----
pytest>=7.0
httpx>=0.27.0                    # Async HTTP client for FastAPI TestClient

# ---- Optional: deployment utilities ----
gunicorn>=22.0.0                 # Production WSGI server (use with uvicorn workers)
